import UI.HomeUI;

public class Main {
    // The main method must be public, static, and take an array of Strings as an argument
    public static void main(String[] args) {
        // This creates the object and opens your UI
        new HomeUI();
    }
}